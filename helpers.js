//regular function

/*
function sum(a, b){
    return a + b;
}
*/

// arrow function many lines

/*
const sum = (a, b) => {
    return a + b;
}
*/

// arrow function one line

const sum = (a, b) => a + b;

// export the functions outside

module.exports = {
    sum
};